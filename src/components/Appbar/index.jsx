import logo from "../../images/logo.svg";

const Appbar = () => {
    return (
        <div className="appbar">
            <img src={logo} className="icon" alt="itb-logo" />
        </div>
    );
};

export default Appbar;
